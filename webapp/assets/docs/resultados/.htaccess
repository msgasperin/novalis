# Bloquear el acceso directo a todos los archivos de esta carpeta
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

# Compatibilidad por si el servidor de producción es más antiguo (Apache 2.2)
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>